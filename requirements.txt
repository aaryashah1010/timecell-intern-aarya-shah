# Task 1 (Risk Engine) uses only the Python standard library at runtime.
# Task 2 (Market Data) needs:
yfinance>=0.2.40
requests>=2.31

# Task 3 (AI Advisor) needs:
openai>=1.40
python-dotenv>=1.0

# Test dependency:
pytest>=7.0
